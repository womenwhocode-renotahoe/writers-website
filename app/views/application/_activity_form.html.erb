<%= form_for [@writer, @activity] do |f| %>
  Today I 

  <%= f.collection_select(:goal_id, @writer.goals, :id, :verb_past_tense) %>
  
  <%= f.number_field :count %>
  <%= f.hidden_field :date, :value => DateTime.now %>
  <%= f.submit %>

<% end %>