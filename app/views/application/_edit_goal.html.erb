<%= form_for [@writer, @goal], remote: true, html: {id: "goal-form" } do |f| %>
	<%= f.label "Count" %>
	<%= f.text_field :count %>
	<%= f.label "Days" %>
	<%= f.text_field :freq %>

	<%= f.submit %>
<% end %>